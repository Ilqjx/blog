spring:
  thymeleaf:
    mode: HTML
    prefix: classpath:/templates/
    suffix: .html
    cache: false
  profiles:
    active: dev # 指定当前使用哪个配置文件(后缀即可)
  messages:
    basename: i18n/messages

comment.avatar: /images/avatar.jpeg